import { defineStore } from "pinia";

export const useStoreApp = defineStore("storeApp", {
  state: () => ({
    app: {
      name: "VNPT Media",
    },
  }),
  state: () => {
    return {
      app: {
        name: "VNPT Media",
      },
    };
  },
  getters: {},
  actions: {},
});
