import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
  state: {
    app: {
        name: "VNPT Media"
    }
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {}
});

export default store;